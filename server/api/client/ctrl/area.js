"use strict";

const AreaService = require('../service/area');
const AreaDao = require('../dao/area');

module.exports = class AddressCtrl {

  static list(req, res) {
    let type = req.body.type;
    let provinceId = req.body.provinceId;
    let cityId = req.body.cityId;

    
  }

}
