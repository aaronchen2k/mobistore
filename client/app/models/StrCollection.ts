import {StrClient} from './StrClient';
import {StrProduct} from './StrProduct';

export class Category{
  constructor(
	collectTime: Date,
  enabled: boolean,

	client: StrClient,
	product: StrProduct
  ) {}
}
