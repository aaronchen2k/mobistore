import {StrProduct} from './StrProduct';

export class Category{
  constructor(
  id: number,
	areaName: string,
	shortName: string,
	lng: string,
	lat: string,
	level : number,
  position: string,
  sort: number,
  enabled: boolean,

  parentId: number
  ) {}
}

